import { extendTheme } from '@chakra-ui/react';

const theme = extendTheme({
  colors: {
    background: '#18191a',
    text: '#e3e3e3',
    primary: '#ed2a4c',
    button: '#000000',
  },
});

export default theme;
